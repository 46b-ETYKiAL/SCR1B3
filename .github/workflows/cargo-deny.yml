# SCR1B3 supply-chain gate — Phase 21 T21.1 primary safety check. Runs
# `cargo-deny` against the workspace `deny.toml` config (license allowlist +
# advisory deny + sources lockdown). Fails the CI on any new license drift,
# unpatched RUSTSEC advisory, or banned source. The in-app updater and the
# F0RG3-W1R3 installer manifest both trust this gate as the primary
# supply-chain proof.

name: cargo-deny

on:
  push:
    branches: [master]
  pull_request:
  schedule:
    # Nightly so advisories that landed between PRs surface within 24 h.
    - cron: "30 6 * * *"
  workflow_dispatch:

concurrency:
  group: cargo-deny-${{ github.ref }}
  # Cancel only stale PR runs; never cancel master/scheduled runs, so they
  # complete and never leave a cancelled (failed-looking) check-run on the
  # commit (a cancelled SARIF/security scan is also an incomplete analysis).
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  deny:
    name: cargo-deny ${{ matrix.checks }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        checks:
          - advisories
          - bans
          - licenses
          - sources
    steps:
      - name: Harden the runner (egress block)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          # Block-mode egress firewall (StepSecurity Community tier is free +
          # fully functional on public repos; ≥ v2.16.0 closes the DoH/UDP
          # bypass CVEs). This scanner job runs on untrusted PR triggers, so a
          # compromised dependency/action cannot exfiltrate — only the listed
          # hosts are reachable. Endpoints: cargo-deny-action fetches its binary
          # (github releases → objects), clones RustSec/advisory-db (github), and
          # reads the crates index/toolchain. GitHub Actions cache + OIDC hosts
          # are auto-detected by harden-runner and need not be listed.
          egress-policy: block
          disable-sudo: true
          allowed-endpoints: >
            github.com:443
            api.github.com:443
            objects.githubusercontent.com:443
            codeload.github.com:443
            raw.githubusercontent.com:443
            index.crates.io:443
            static.crates.io:443
            crates.io:443
            static.rust-lang.org:443
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25 # v2.1.1
        with:
          command: check ${{ matrix.checks }}
          arguments: --workspace --all-features
